dependencies:
    pre:
        - wget -qO- https://get.docker.com/ | sh
        - sudo restart now
        - sudo apt-get update; sudo apt-get install btrfs-tools; btrfs --version

test:
    override:
        - scripts/launch_test
